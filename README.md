# Telecom-Customer-Spending-Prediction-with-Machine-Learning-Neural-Networks
Predict customer total spending using telecom churn data. The project includes data cleaning, preprocessing, encoding, scaling, and building two predictive models: Linear Regression and a Keras deep neural network. Both models are evaluated using MSE to compare accuracy and performance.
